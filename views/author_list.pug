extends layout 

block content
    h1=title

    ul
        each author in list_author
            li
                a(href=author.url) #{author.name}
                |   #{author.lifespan}
        else
            li There are no authors